import React from "react";
// import { useState } from "react";
// import axios from "axios";

export function ShowPost(props) {
    // const { commentsItem, setComments } = useState([])

    // axios.get("http://localhost:3004/api/get-comments/" + props.Id)
    //     .then(res => {

    //     })

    return (
        <div>
            <h1>{props.Title}</h1>

        </div>
    )
}